{
  "type": "object",
  "properties": {
    "overall_status": { "type": "string", "enum": ["OK", "WARN", "CRIT"] },
    "summary": { "type": "string" },
    "services": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "name": { "type": "string" },
          "status": { "type": "string", "enum": ["OK", "WARN", "CRIT"] },
          "finding": { "type": "string" }
        },
        "required": ["name", "status", "finding"]
      }
    },
    "recommendations": {
      "type": "array",
      "items": { "type": "string" }
    }
  },
  "required": ["overall_status", "summary", "services", "recommendations"]
}
